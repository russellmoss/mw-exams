# Environment and secrets
.env
.env.*
!.env.example

# Build plan (per user request — keep local only)
BUILD_PLAN.md
mw_build_plan.md
*build_plan*.md
MW_Build_Plan.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo

# Claude Code local artifacts (not the agents themselves)
.claude/local/
.claude/cache/

# Outputs that are large or regenerable
outputs/**/*.draft.md

# Tour videos: hundreds of MB of MP4, rebuilt by study-app/scripts/record-tour-videos.mjs from the
# committed narration and the live components. The frame scratch dir is mid-render state.
outputs/tour_videos/
tour_videos/
study-app/.tour-video-work/

# Logs
*.log
logs/
.vercel

# Generated Neon corpus sync SQL (~1MB, rebuild with scripts/gen_neon_corpus_sql.py)
data/structured/_neon_statements.json
study-app/.tmp/

# Raw IMW source PDFs — copyrighted IMW material, ~14MB of binaries.
# Reproduce with: python scripts/fetch_imw_pdfs.py
source/imw_pdfs/
