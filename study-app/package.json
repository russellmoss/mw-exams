{
  "name": "study-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "migrate": "node scripts/migrate.mjs",
    "prebuild": "node scripts/sync-theory-data.mjs && npm run test:build-gate && node scripts/migrate.mjs && node scripts/sync-stem-data.mjs && node scripts/build-app-surface.mjs && node -e \"const fs=require('fs'); if(!fs.existsSync('public/data/pipeline-context.json')){console.log('Building data index...'); require('child_process').execSync('node ../scripts/build-question-index.js',{stdio:'inherit'})} else {console.log('Data index exists, skipping prebuild')}\"",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "test:build-gate": "vitest run --exclude '**/*.eval.test.ts' --exclude '**/*.integration.test.ts'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.99.0",
    "@neondatabase/serverless": "^1.1.0",
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3",
    "mermaid": "^11.15.0",
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-markdown": "^10.1.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
