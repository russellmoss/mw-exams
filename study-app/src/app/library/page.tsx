import type { Metadata } from "next";
import { WalkthroughReplayButtons } from "@/app/components/WalkthroughReplayButton";

export const metadata: Metadata = { title: "Library" };

// Library (docs/design/2026-08-06-shell-redesign/ §12) — the diagrams/decision-tree study library,
// renamed from /diagrams. The content is the prebuilt static site (public/diagrams/, generated by
// scripts/build_study_diagrams_site.py) embedded inside the app shell so it shares the real NavBar.
// Internal links/assets stay absolute (/diagrams/*) and navigate within the iframe.
export default function LibraryPage() {
  return (
    <div className="flex flex-col flex-1">
      <header className="border-b border-border">
        <div className="max-w-5xl mx-auto px-6 py-6 w-full flex items-start justify-between gap-4">
          <div>
            <h1 className="text-2xl font-bold text-foreground tracking-tight">Library</h1>
            <p className="text-sm text-muted mt-1">
              Study diagrams and decision trees. Learning units coming soon.
            </p>
          </div>
          <WalkthroughReplayButtons />
        </div>
      </header>
      <iframe
        src="/diagrams/index.html"
        title="MW Study Library"
        // Explicit height (viewport minus the ~50px NavBar and ~85px page header) so the iframe
        // always resolves to a definite size and scrolls its content.
        className="block w-full border-0"
        style={{ height: "calc(100dvh - 135px)" }}
      />
    </div>
  );
}
